<form method="post" action="{{ path('app_admin_adresse_delete', {'id': adresse.id}) }}" onsubmit="return confirm('Etes vous sûr de vouloir supprimer??');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ adresse.id) }}">
    <button class="btn">Delete</button>
</form>
