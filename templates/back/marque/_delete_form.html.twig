<form method="post" action="{{ path('app_admin_marque_delete', {'id': marque.id}) }}" onsubmit="return confirm('Etes vous sûr de vouloir supprimer?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ marque.id) }}">
    <button class="btn btn-danger col-md-12">Supprimer</button>
</form>
