<!DOCTYPE html>

<title>New Panier</title>

{% block body %}
    <h5>Create new Panier</h5>
    {{ include('back/commande/_form.html.twig') }}
    <a href="{{ path('app_admin_commande_index') }}">back to list</a>
{% endblock %}
